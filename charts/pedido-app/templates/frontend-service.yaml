apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: pedidos
    component: frontend

spec:
  type: ClusterIP

  selector:
    app: frontend

  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80