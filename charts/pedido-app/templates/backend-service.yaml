apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: pedidos
    component: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8080